import { createClient } from '@/lib/supabase/server'; import { redirect } from 'next/navigation'; import { Sidebar } from '@/components/sidebar'; import { PageWorkspace } from '@/components/editor/page-workspace';
export default async function PageById({ params }: { params: Promise<{id:string}> }) { const {id}=await params; const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect('/login'); const {data:pages}=await supabase.from('pages').select('id,title').eq('user_id',user.id); const {data:page}=await supabase.from('pages').select('*').eq('id',id).eq('user_id',user.id).single(); if(!page) redirect('/dashboard');
 return <main className='flex'><Sidebar pages={pages??[]}/><section className='p-6 w-full'><PageWorkspace page={page}/></section></main>; }
